[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "your-bot-name"
version = "0.1.0"
dependencies = [
    "python-telegram-bot==20.7",
    "python-dotenv>=1.0.0"
]
